// 6=> for each loop 
// forEach() is a built-in method for arrays in JavaScript.
// It is used to run a function for every element in the array — from start to end
let nums = [10, 20, 30]
nums.forEach((num) => {
 console.log(num)
});
// sybtax of for each is different 
// array.forEach(function(element, index, array) {
// code to run for each element
// });
let numbers = [10, 20, 30, 40, 50];

numbers.forEach(function(num) {
  console.log(num);
});